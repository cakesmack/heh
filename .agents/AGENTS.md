# Workspace Rules

- **Database Model Migrations**: Never alter a database model without explicitly generating the corresponding Alembic migration command (`alembic revision --autogenerate -m "..."`).
